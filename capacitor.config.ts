import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'vite-ionic6-vue3-ts',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
